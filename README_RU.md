# 🛡️ VPS Backup Scripts

> Профессиональные автоматизированные решения для резервного копирования VPS серверов с функциями enterprise-уровня

![Bash](https://img.shields.io/badge/Bash-4EAA25?style=for-the-badge&logo=gnu-bash&logoColor=white)
![Linux](https://img.shields.io/badge/Linux-FCC624?style=for-the-badge&logo=linux&logoColor=black)
![MySQL](https://img.shields.io/badge/MySQL-4479A1?style=for-the-badge&logo=mysql&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-4169E1?style=for-the-badge&logo=postgresql&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green.svg)
![GitHub](https://img.shields.io/badge/GitHub-13winged-blue?style=for-the-badge&logo=github)

Комплексное решение для резервного копирования VPS серверов, обеспечивающее надежное автоматическое резервное копирование системных файлов, баз данных и веб-сервисов с проверкой целостности и мониторингом.

## ✨ Возможности

| Категория | Функции |
|----------|----------|
| **🔧 Системный бэкап** | Конфиги системы, домашние директории, списки пакетов, данные пользователей |
| **🗄️ Бэкап БД** | Поддержка MySQL & PostgreSQL с транзакционно-безопасными дампами |
| **🌐 Веб-сервисы** | Файлы сайтов, конфиги Nginx/Apache, SSL сертификаты |
| **🔒 Безопасность** | Проверка целостности, верификация SHA256, шифрование бэкапов |
| **⚡ Автоматизация** | Готовность к cron, политики хранения, автоматическая очистка |
| **📊 Мониторинг** | Детальное логирование, email уведомления, проверки здоровья |

## 🚀 Быстрый старт

### Предварительные требования
- VPS на базе Linux
- Bash оболочка
- Доступ root/sudo

### Установка

```bash
# Клонируем репозиторий
git clone https://github.com/13winged/vps-backup-scripts.git
cd vps-backup-scripts

# Даем права на выполнение
chmod +x scripts/*.sh

# Настраиваем параметры бэкапа
cp config.example config
nano config
```

### Первый запуск

```bash
# Тестируем полный бэкап системы
./scripts/backup-vps.sh

# Тестируем бэкап только БД
./scripts/db-backup.sh

# Проверяем целостность бэкапа
./scripts/integrity-check.sh --latest
```

## 📋 Обзор скриптов

### `backup-vps.sh` - Полный бэкап системы
```bash
./scripts/backup-vps.sh
```
**Что бэкапит:**
- ✅ Системные конфигурации (`/etc`)
- ✅ Данные пользователей (`/home`, `/root`)
- ✅ Списки пакетов и системная информация
- ✅ Веб-директории и конфиги
- ✅ Дампы баз данных (MySQL/PostgreSQL)

### `db-backup.sh` - Бэкап баз данных
```bash
./scripts/db-backup.sh
```
**Возможности:**
- 🗄️ Транзакционно-безопасные дампы MySQL
- 🗄️ Бэкапы PostgreSQL баз данных
- 📦 Автоматическое сжатие
- 🔄 Поддержка инкрементальных бэкапов

### `integrity-check.sh` - Проверка бэкапов
```bash
./scripts/integrity-check.sh --latest
./scripts/integrity-check.sh --databases
./scripts/integrity-check.sh --file /backup/file.tar.gz
```
**Проверки:**
- 🔍 Валидация структуры архивов
- 🔐 Верификация SHA256 контрольных сумм
- 🗄️ Тестирование целостности дампов БД
- 🧪 Проверка возможности восстановления

## ⚙️ Настройка

Отредактируйте файл `config` под вашу среду:

```bash
# ===== ОСНОВНЫЕ НАСТРОЙКИ =====
BACKUP_DIR="/backup"
RETENTION_DAYS=7

# ===== ЦЕЛИ БЭКАПА =====
BACKUP_SYSTEM_FILES=true
BACKUP_WEB_DIRS=true
BACKUP_MYSQL=true
BACKUP_POSTGRES=true

# ===== ПРОВЕРКА ЦЕЛОСТНОСТИ =====
ENABLE_INTEGRITY_CHECK=true
VERIFY_CHECKSUMS=true

# ===== НАСТРОЙКИ БАЗ ДАННЫХ =====
MYSQL_USER="root"
MYSQL_HOST="localhost"
PG_USER="postgres"
PG_HOST="localhost"
```

## 📅 Автоматизация

### Настройка Cron
```bash
# Редактируем crontab
crontab -e

# Добавляем записи:
# Полный бэкап системы ежедневно в 2:00
0 2 * * * /opt/vps-backup-scripts/scripts/backup-vps.sh

# Бэкап БД каждые 6 часов
0 */6 * * * /opt/vps-backup-scripts/scripts/db-backup.sh

# Проверка целостности ежедневно в 4:00
0 4 * * * /opt/vps-backup-scripts/scripts/integrity-check.sh --latest

# Очистка старых бэкапов еженедельно в воскресенье
0 3 * * 0 find /backup -name "*.tar.gz" -mtime +7 -delete
```

### Systemd сервис (альтернатива)
Создайте `/etc/systemd/system/vps-backup.service`:
```ini
[Unit]
Description=VPS Backup Service
After=network.target

[Service]
Type=oneshot
User=root
ExecStart=/opt/vps-backup-scripts/scripts/backup-vps.sh
```

## 🗂️ Структура бэкапов

```
/backup/
├── 📁 vps-backup-2001-01-11_01-00-01.tar.gz
├── 📁 databases/
│   └── 📁 2001-01-01/
│       ├── 📁 mysql/
│       │   ├── 📄 website_db.sql.gz
│       │   └── 📄 app_db.sql.gz
│       └── 📁 postgresql/
│           └── 📄 cms_db.sql.gz
└── 📁 logs/
    ├── 📄 vps-backup.log
    ├── 📄 db-backup.log
    └── 📄 integrity-check.log
```

## 🔧 Руководство по восстановлению

### Аварийное восстановление
```bash
# 1. Распаковываем бэкап
tar -xzf vps-backup-2024-01-15_02-00-01.tar.gz

# 2. Восстанавливаем MySQL базы
for db_file in mysql/*.sql.gz; do
    db_name=$(basename "$db_file" .sql.gz)
    echo "Восстанавливаем MySQL: $db_name"
    zcat "$db_file" | mysql -u root -p
done

# 3. Восстанавливаем PostgreSQL базы
for db_file in postgresql/*.sql.gz; do
    db_name=$(basename "$db_file" .sql.gz)
    echo "Восстанавливаем PostgreSQL: $db_name"
    zcat "$db_file" | sudo -u postgres psql
done

# 4. Восстанавливаем системные файлы
tar -xzf system/etc.tar.gz -C /
tar -xzf system/home.tar.gz -C /
```

### Выборочное восстановление
```bash
# Восстанавливаем только конкретную базу
zcat mysql/wordpress.sql.gz | mysql -u root -p wordpress

# Восстанавливаем только файлы сайта
tar -xzf web/var-www.tar.gz -C /

# Восстанавливаем только конфигурацию
tar -xzf system/etc.tar.gz -C /
```

## 📊 Мониторинг и логи

### Расположение лог-файлов
```bash
/var/log/vps-backup.log      # Логи полного бэкапа
/var/log/db-backup.log       # Логи бэкапа БД
/var/log/integrity-check.log # Логи проверки целостности
```

### Мониторинг в реальном времени
```bash
# Следим за прогрессом бэкапа
tail -f /var/log/vps-backup.log

# Проверяем статус бэкапа
grep -i "completed\|error\|failed" /var/log/vps-backup.log

# Мониторим использование диска
watch df -h /backup
```

### Проверки здоровья
```bash
# Проверяем последний бэкап
./scripts/integrity-check.sh --latest

# Проверяем возраст бэкапов
find /backup -name "*.tar.gz" -mtime -1

# Проверяем бэкапы БД
./scripts/integrity-check.sh --databases
```

## 🛡️ Рекомендации по безопасности

### Права доступа к файлам
```bash
# Защищаем конфигурационный файл
chmod 600 config
chown root:root config

# Защищаем директорию бэкапов
chmod 700 /backup
chown root:root /backup
```

### Шифрование (опционально)
```bash
# Шифруем бэкапы с помощью GPG
tar -czf - /path/to/backup | gpg --encrypt --recipient user@domain.com > backup.tar.gz.gpg

# Дешифруем для восстановления
gpg --decrypt backup.tar.gz.gpg | tar -xzf -
```

### Безопасность удаленных бэкапов
```bash
# Используем SSH ключи для удаленных передач
ssh-keygen -t rsa -b 4096 -C "backup@vps"
ssh-copy-id backup@remote-server

# Безопасные бэкапы в S3
aws s3 cp backup.tar.gz s3://my-backup-bucket/ --sse AES256
```

## 🐛 Решение проблем

### Частые проблемы и решения

| Проблема | Решение |
|---------|----------|
| **Permission denied** | Запускайте с `sudo` или проверьте права пользователя |
| **Доступ к MySQL запрещен** | Проверьте привилегии и учетные данные пользователя MySQL |
| **Место на диске закончилось** | Увеличьте дни хранения или очистите старые бэкапы |
| **Бэкап слишком медленный** | Настройте уровень сжатия или исключите большие файлы |
| **Ошибка подключения к БД** | Проверьте, запущена ли служба базы данных |

### Режим отладки
```bash
# Включаем подробный вывод
bash -x ./scripts/backup-vps.sh

# Проверяем конкретные ошибки
./scripts/backup-vps.sh 2>&1 | grep -i "error\|warning\|failed"

# Тестируем конфигурацию
./scripts/backup-vps.sh --dry-run
```

### Анализ логов
```bash
# Ищем ошибки в логах
grep -i error /var/log/vps-backup.log

# Мониторим продолжительность бэкапа
grep "completed" /var/log/vps-backup.log

# Проверяем размеры бэкапов
ls -lh /backup/*.tar.gz
```

### Настройка среды разработки
```bash
# Клонируем и настраиваем
git clone https://github.com/13winged/vps-backup-scripts.git
cd vps-backup-scripts

# Создаем тестовую среду
mkdir -p test-backup
export BACKUP_DIR="$PWD/test-backup"

# Запускаем тесты
./scripts/backup-vps.sh
./scripts/integrity-check.sh --latest
```

### Сообщение об ошибках
- Используйте страницу GitHub Issues
- Включите информацию о вашей системе и конфигурации
- Предоставьте соответствующие лог-файлы
- Опишите шаги для воспроизведения

## 📝 Лицензия

Этот проект распространяется под лицензией MIT - см. файл [LICENSE](LICENSE) для подробностей.

## ⚠️ Важное предупреждение

**🚨 ВАЖНО: Всегда тестируйте ваши бэкапы!**

> Бэкап полезен только если его можно восстановить. Регулярно тестируйте процедуру восстановления бэкапа в безопасной изолированной среде, чтобы убедиться, что ваши данные можно восстановить когда это потребуется.

### Чеклист тестирования
- [ ] Регулярно проверяйте целостность бэкапов
- [ ] Тестируйте процесс восстановления ежеквартально
- [ ] Храните несколько версий бэкапов
- [ ] Храните бэкапы в отдельных местах
- [ ] Документируйте процедуры восстановления

---